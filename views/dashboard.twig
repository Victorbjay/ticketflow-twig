{% extends "layout.twig" %}

{% block title %}Dashboard{% endblock %}

{% block content %}
<div class="dashboard">
    <!-- Navigation -->
    <nav class="nav">
        <div class="nav-container">
            <div class="nav-brand">ResolveHub</div>
            <div class="nav-actions">
                <span class="user-email">{{ user.email }}</span>
                <a href="/logout" class="btn-logout">Logout</a>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="container">
        <!-- Welcome Section -->
        <div class="welcome">
            <div class="circle"></div>
            <h1>Welcome Back 👋</h1>
            <p>Here's an overview of your tickets</p>
        </div>

        <!-- Stats Grid -->
        <div class="stats-grid">
            <div class="stat-card" style="border-color: #1e40af33;">
                <div class="stat-circle" style="background: radial-gradient(circle, #1e40af33, transparent);"></div>
                <div class="stat-icon">📊</div>
                <div class="stat-value" style="color: #3b82f6;">{{ stats.total }}</div>
                <div class="stat-label">Total Tickets</div>
            </div>

            <div class="stat-card" style="border-color: #10b98133;">
                <div class="stat-circle" style="background: radial-gradient(circle, #10b98133, transparent);"></div>
                <div class="stat-icon">🟢</div>
                <div class="stat-value" style="color: #10b981;">{{ stats.open }}</div>
                <div class="stat-label">Open Tickets</div>
            </div>

            <div class="stat-card" style="border-color: #fbbf2433;">
                <div class="stat-circle" style="background: radial-gradient(circle, #fbbf2433, transparent);"></div>
                <div class="stat-icon">🟡</div>
                <div class="stat-value" style="color: #fbbf24;">{{ stats.in_progress }}</div>
                <div class="stat-label">In Progress</div>
            </div>

            <div class="stat-card" style="border-color: #94a3b833;">
                <div class="stat-circle" style="background: radial-gradient(circle, #94a3b833, transparent);"></div>
                <div class="stat-icon">✓</div>
                <div class="stat-value" style="color: #94a3b8;">{{ stats.closed }}</div>
                <div class="stat-label">Closed</div>
            </div>
        </div>

        <!-- Quick Actions -->
        <div class="quick-actions">
            <h2>Quick Actions</h2>
            <a href="/tickets" class="btn-action">
                <span>🎫</span> Manage Tickets
            </a>
        </div>
    </div>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <p>© 2025 ResolveHub • Built for Frontend Wizards Stage 2</p>
        </div>
    </footer>
</div>
{% endblock %}